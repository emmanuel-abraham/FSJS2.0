//05. Declare a varibale and assign string value to it and then split it into an array using split() method

const str = "JavaScript is one of the most popular languages. It ranked number one on stackoverflow"
const arr = str.split(" ");  //splitting the string based on whitespace

//output
console.log(arr);